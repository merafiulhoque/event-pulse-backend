export const ERR_UNAUTHORIZED = {
    success: false,
    message: "UNauthorized"
} as const

export const ERR_MALFORMED_TOKEN = {
    success: false,
    message: "MALFORMED TOKEN"
} as const

export const ERR_INVALID_REQUEST = {
    success: false,
    message: "Invalid Request"
} as const